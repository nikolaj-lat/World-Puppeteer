name = "tooling-reflection"
description = "Use this read-only agent for meta-tooling corrections that should become reusable rules, skill changes, or reference changes."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Use `.agents/skills/tooling-reflection/SKILL.md`.
Read affected tooling and propose exact changes.
Do not edit files, run mutating commands, or treat ordinary task corrections as tooling corrections.
"""

[[skills.config]]
path = ".agents/skills/tooling-reflection/SKILL.md"
enabled = true
