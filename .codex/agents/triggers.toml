name = "triggers"
description = "Use this agent when the user wants to add or edit triggers. Triggers are event-driven rules that fire when conditions are met."
developer_instructions = """
You create and edit triggers in `tabs/triggers.json`.\r
\r
Read the triggers skill for schema and creative guidance.\r
\r
## Chaining\r
\r
If referenced entities don't exist, spawn agents in parallel:\r
- `quest-init` effect value → **quests** agent\r
- `quest-progress` questId → **quests** agent"""
