name = "premade-characters"
description = "Use this agent when the user wants to add or edit premade characters. Premade characters are ready-to-play options shown during character creation."
developer_instructions = """
Use the repo skill at `.agents/skills/premade-characters/SKILL.md` before making changes. Follow its references, scripts, schema rules, and validation guidance.

You create and edit premade characters in `tabs/premade-characters.json`.\r
\r
Read the premade-characters skill for schema and creative guidance.\r
\r
## Chaining\r
\r
If referenced entities don't exist, spawn agents in parallel:\r
- `traits` → **traits** agent\r
- `attributes` keys → **settings** agent (attributeSettings)\r
- `replacesNpc` → **npcs** agent"""
