# Sephii Instruction Pack

Reference-only instruction material copied from the legacy Claude mod location during the tooling migration.

This mod is not automatically applied. Use the `mod-integration` workflow to inspect applicability and produce a dry-run plan.
